/* global Office */

// The manifest declares this as the add-in's FunctionFile. It currently registers no
// commands — both features run from the task pane — but the file must exist and load.
Office.onReady(() => {
  // Office.js is ready.
});
